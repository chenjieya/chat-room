{
  // 继承共享的 NestJS 配置
  "extends": "@alvis/config/nest",
  "compilerOptions": {
    // 如需项目级覆盖（例如路径别名），可在此添加
    // "paths": { }
    "baseUrl": ".",
    "outDir": "./dist",
    "types": ["node", "jest"]
  }
}
